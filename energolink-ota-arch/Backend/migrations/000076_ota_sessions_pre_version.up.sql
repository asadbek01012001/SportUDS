-- KAN-43 (коммент Elbek): health-check ложно ставил «откат» — сравнивал версию из cmd ("0.2")
-- с версией из OTA/info ("0.0.900"), форматы разные. Решение: фиксируем версию устройства НА МОМЕНТ
-- создания сессии (pre-OTA baseline из OTA/info), а успех определяем по ФАКТУ изменения версии
-- (новая != прежней), а не по точному совпадению строки.
ALTER TABLE ota_sessions ADD COLUMN IF NOT EXISTS pre_ota_version TEXT;
